% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_commute_stats.R
\name{compute_commute_stats}
\alias{compute_commute_stats}
\title{Compute commute flow statistics from LODES OD data}
\usage{
compute_commute_stats(od_df, agg_geo = "tract")
}
\arguments{
\item{od_df}{A data frame (tibble) of LODES origin-destination data
returned by \code{\link[=grab_lodes]{grab_lodes()}} with \code{lodes_type = "od"}. Must include
columns \verb{h_\{agg_geo\}} (home geography), \verb{w_\{agg_geo\}} (work geography),
and \code{S000} (total job count). The data frame may be at any aggregation
level supported by \code{agg_geo}.

\strong{Note on row structure:} LODES OD files are a flow matrix. A call to
\code{\link[=grab_lodes]{grab_lodes()}} with \code{lodes_type = "od"} returns one row per
\emph{origin-destination pair}, not one row per geography -- even after
aggregation via \code{agg_geo}. For example, a county-level OD pull for West
Virginia returns ~2,800 rows (one per observed county-county flow pair),
not 55 (the number of counties). Pass the result directly to
\code{compute_commute_stats()} to reduce the pair table to one row per
geography with inflow, outflow, net flow, and self-containment.}

\item{agg_geo}{The geographic level of the OD data. Must match the level
at which \code{od_df} was retrieved or aggregated. One of \code{"block"}, \code{"bg"},
\code{"tract"}, \code{"county"}, or \code{"state"}. Defaults to \code{"tract"}.}
}
\value{
A tibble with one row per geography, containing:
\describe{
\item{\code{{agg_geo}}}{The geographic identifier.}
\item{\code{year}}{Year of the data (if present in \code{od_df}).}
\item{\code{state}}{State FIPS abbreviation (if present in \code{od_df}).}
\item{\code{workers_in}}{Total workers arriving (working in this geography).}
\item{\code{workers_out}}{Total workers departing (living here, working elsewhere).}
\item{\code{workers_internal}}{Workers whose home and work are both in this geography (internal flows).}
\item{\code{net_flow}}{Net worker flow: \code{workers_in - workers_out}. Positive values indicate net job importers.}
\item{\code{self_containment}}{Share of resident workers who also work in this geography: \code{workers_internal / workers_out_total}, where \code{workers_out_total} includes internal flows.}
}
}
\description{
Derives three key commute flow metrics from an OD tibble:
inflow, outflow, net flow, and the self-containment ratio. These metrics
are widely used in transportation planning and economic geography to
characterize labor market catchment areas and job/housing balance.

Self-containment is defined as the proportion of workers who both live
and work within the same geographic unit, relative to all employed
residents. A value close to 1 indicates a highly self-contained labor
market; values near 0 indicate heavy out-commuting.

Net flow is defined as inbound workers minus outbound workers (including
internal flows as both in and out). This is an unsigned flow balance
indicator: positive values signal net job importers (more workers arrive
than leave); negative values signal net exporters.

\strong{Cross-state commuters:} When \code{state_part = "main"} is used in
\code{\link[=grab_lodes]{grab_lodes()}}, only workers who live and work in the same state are
included. Workers who cross state lines (e.g., Maryland residents working
in DC) appear only in \code{state_part = "aux"} files for the \emph{workplace}
state. To capture full commute flows for border counties, retrieve both
\code{"main"} and \code{"aux"} files and bind the rows before calling
\code{compute_commute_stats()}.
}
\examples{
\donttest{
  od <- grab_lodes(
    state = "md", year = 2019,
    lodes_type = "od", job_type = "JT00",
    segment = "S000", state_part = "main",
    agg_geo = "county"
  )
  compute_commute_stats(od, agg_geo = "county")
}
}
