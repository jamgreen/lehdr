% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_lodes_change.R
\name{compute_lodes_change}
\alias{compute_lodes_change}
\title{Compute longitudinal change in LODES data across years}
\usage{
compute_lodes_change(
  lodes_df,
  geo_col = NULL,
  base_year = NULL,
  compare_year = NULL,
  variables = NULL,
  output = c("wide", "long")
)
}
\arguments{
\item{lodes_df}{A data frame (tibble) of LODES data covering at least two
years, as returned by \code{\link[=grab_lodes]{grab_lodes()}} with a vector of years. Must
contain a \code{year} column and one or more numeric columns to difference.}

\item{geo_col}{The name of the geography column to group by, e.g.
\code{"w_tract"}, \code{"h_county"}, or \code{"w_geocode"}. Defaults to \code{NULL}, which
auto-detects the first column ending in \verb{_geocode}, \verb{_tract}, \verb{_county},
\verb{_bg}, or \verb{_state}.}

\item{base_year}{The reference year for computing change. Defaults to the
earliest year present in \code{lodes_df}.}

\item{compare_year}{The target year for computing change. Defaults to the
latest year present in \code{lodes_df}.}

\item{variables}{Optional character vector of numeric column names to
include in the output. Defaults to all numeric columns (excluding \code{year}).}

\item{output}{One of \code{"wide"} (default) or \code{"long"}. In wide format,
absolute and percentage change columns are appended for each variable. In
long format, each variable is a row with columns \code{variable},
\code{base_value}, \code{compare_value}, \code{change}, and \code{pct_change}.}
}
\value{
A tibble of change statistics. In \code{"wide"} format, columns follow
the pattern \verb{\{variable\}_base}, \verb{\{variable\}_compare},
\verb{\{variable\}_change}, and \verb{\{variable\}_pct_change}. In \code{"long"} format,
columns are \code{variable}, \code{base_value}, \code{compare_value}, \code{change}, and
\code{pct_change}.
}
\description{
Computes absolute and percentage change in LODES variables
between two years for each geographic unit. This is particularly useful
for tracking shifts in employment, industrial composition, or earnings
structure over time.

Percentage change is computed as
\eqn{(\text{compare} - \text{base}) / \text{base} \times 100}.
Returns \code{NA} where \code{base_value} is zero or missing.
}
\examples{
\donttest{
  wac_multi <- grab_lodes(
    state = "md", year = c(2015, 2019),
    lodes_type = "wac", job_type = "JT00",
    segment = "S000", agg_geo = "county"
  )
  compute_lodes_change(wac_multi, geo_col = "w_county")

  # Long format
  compute_lodes_change(
    wac_multi,
    geo_col  = "w_county",
    output   = "long",
    variables = c("C000", "CE01", "CE02", "CE03")
  )
}
}
